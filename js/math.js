//the pie
let chart = Math.PI;
console.log(chart);// returns the value of pie

//turning a number to an integer
// method one is the math.round method
const round = 78.09999;
let rounded = Math.round(round);
console.log(rounded);

//math.ceil
let ceiled = Math.ceil(round);// returns a number rounded to its nearest integer
console.log(ceiled);

let num = 12.00099999;
let numCeiled = Math.ceil(num);//returns the number rounded to its nearest integer
console.log(numCeiled);

//math.floor

const number = 77.89;
let floored = Math.floor(number);// returns a number without decimal places
console.log(floored);//removes the decimals

//math.trunc method

let trunced = Math.trunc(number);
console.log(trunced);// returns the integer part of the number

//math.sign

let signs = Math.sign(number);
console.log(signs);

let sign = Math.sign(-5);
console.log(sign);

// the math.pow

let power = Math.pow(25,5);
console.log(power);//returns the value of 25 power 5

//the sqrt method

let root = Math.sqrt(25);//returns the squareroot of the persed number in the method
console.log(root);

let roots = Math.sqrt(100);//returns the squareroot of the persed number in the method
console.log(roots);

//the abs method
let absolute = Math.abs(-78);// returns the positive number of the negative persed
console.log(absolute);

//the sin method

let sine = Math.sin(20 * Math.PI / 180);// returns the sine of 20
console.log(sine);


//the cos method

let cosine = Math.cos(20 * Math.PI / 180);//returns the cosine of 20
console.log(cosine);

//math.min and max 

let mini = Math.min(12, 34, 56, 7, 89);
console.log(mini);//returns the minimum number from the list

//math.min and max 

let max = Math.max(12, 34, 56, 7, 89);
console.log(max);//returns the maximum number from the list

// the math . random

let g = Math.random();//returns a number btn 0 inclusive and 1 exclusive
console.log(g);

//math.log
let k = Math.log(7);//returns the logarithm of the  persed number
console.log(k);

//math.log
let o = Math.log2(2);//returns the logarithm of the  persed number
console.log(o);


//math.log
let kk = Math.log2(4);//returns the logarithm of the  persed number
console.log(kk);


//math.log
let ten = Math.log10(2000);//returns the logarithm of the  persed number
console.log(ten);


//math.log
let tenn = Math.log10(100);//returns the logarithm of the  persed number
console.log(tenn);